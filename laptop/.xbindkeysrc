#"ponymix increase 10 && notify-send 'Volume is' $(ponymix get-volume)"
#  XF86AudioRaiseVolume
#
#"ponymix decrease 10 && notify-send 'Volume is' $(ponymix get-volume)"
#  XF86AudioLowerVolume
#
#"ponymix toggle && notify-send $((ponymix is-muted && echo 'Volume is muted') || echo 'Volume is not muted')"
#  XF86AudioMute
#
"xbacklight -inc 10"
  XF86MonBrightnessUp

"xbacklight -dec 10"
  XF86MonBrightnessDown

"systemctl suspend"
  Alt + End

"thunar"
  Control + Home

"scrot -m -e 'mv $f ~/pictures/screens/'"
  Print

"termite -e ncmpcpp"
  Control + Alt + m

"chromium"
  Control + Alt + W

"dmenu_run"
  Mod4 + p

"passmenu"
  Mod4 + Shift + p

"termite -e 'bc -q'"
  Control + Alt + c
